# 本周开发归档 · 团队死斗 与 爆破模式

> 项目：体素战争 Voxel Frontline（Three.js 体素 FPS/RTS）
> 归档日期：2026-03-14
> 范围：本周新增的两个玩法模式（团队死斗、爆破）及其配套 AI 改进

---

## 0. 总览

本周在既有「核心攻防」（`core`）之外，新增并完善了两个可独立选择的玩法模式，均通过
`js/gamemodes.js` 的模式表统一定义，所有规则数值从 `GameModes.param(key)` 单点读取，
代码其余部分不再直接判断模式 id。

| 模式 | id | 队伍人数 | 复活 | 胜负 | 地图 |
|------|-----|---------|------|------|------|
| 团队死斗 | `tdm` | 12 | 即时复活（延迟 3s） | 率先达到 50 分 / 600s 时限 | 随机地图（天际线上限 26） |
| 爆破 | `demo` | 5 | 单命制（回合开始整队重投） | 先赢 7 回合（BO13），半场 6 回合后攻守交换 | 固定地图 + A/B 包点 |

设计主线：**规则、状态、渲染、出生、AI 各自独立成模块**，通过可注入的 hooks 与事件
解耦，保证单一职责、可测试、互不侵入。

---

## 1. 团队死斗（TDM）

### 1.1 规则要点（`gamemodes.js` → `tdm.params`）
- 得分上限 `scoreLimit: 50`，时限 `timeLimit: 600s`，可选加时（当前关闭）。
- 复活：`instantRespawn` + `respawnDelay: 3.0`，出生保护 `spawnProtection: 1.0`。
- 局时结构：准备 `prepTime: 10` → 交战 → 结算 `resultTime: 15`。
- 团队计分 `teamScore`：击杀/爆头 +1，自杀/误伤 −1。
- 个人计分 `personalScore`：击杀 100、爆头 150、助攻 50、自杀 −50、误伤 −100。
- 连杀档位 `killstreakTiers: [3,5,7]` → 三连杀 / 五连杀 / 七连杀。
- 随机地图约束 `mapHeightCap: 26`：压低天际线，缩短交火距离，保证节奏密集。

### 1.2 模块与职责
- `js/tdm-match.js` — 回合状态机（准备/交战/加时/结算）。全局唯一的**带归属的击杀事件**
  `registerKill()`：团队分、个人分、助攻、连杀、击杀滚屏、MVP 全部由此派生，其余模块不再自持计数。
  参与者以 id 表示：本地玩家 `player`、AI 用 `unit.id`、PVP 对手 `remote`。
- `js/tdm-spawn.js` — 即时复活 + **安全评分出生点选择**：对每个候选区计算与敌队距离、
  惩罚被敌人可见的区域、排除队友刚阵亡处，再从最优前几名里随机挑选（`spawnPickTop: 3`），
  使出生点不可预测；含防守方连续阵亡的「反出生点压制」检测。
- `js/tdm-ui.js` — HUD：复用顶栏两条分段条与中央时钟（仅替换数据源），补充击杀滚屏、
  复活倒计时、结算 MVP 表。

### 1.3 解决的问题
- **两队在死区互相僵住**：检测范围（约 60）远大于武器射程（18–36），一「看见」敌人就站定，
  导致对峙冻结。改为「打不到就继续贴近、绕开地标」。
- **出生即被打**：安全评分出生 + 反压制窗口，避免在敌人视线内或队友刚死处刷新。

---

## 2. 爆破模式（Demolition / Search & Destroy）

### 2.1 规则要点（`gamemodes.js` → `demo.params`）
- 5v5 **单命制**：回合内阵亡不复活，仅回合开始整队重投。
- 回合：先赢 `roundsToWin: 7`，`halfSwapAfter: 6` 半场攻守交换，`maxRounds: 13`（BO13）。
- 局时：购买 `buyTime: 15` → 交战 `roundTime: 110` → 结算 `resultTime: 5`；炸弹 `bombTimer: 45`。
- 安装：`plantTime: 4.0`，`siteCount: 2`（A/B），安装键 `KeyE`。
- 拆除：`defuseTime: 7.0`，带拆弹器 `defuseTimeWithKit: 3.5`，范围 `defuseRange: 1.5`，键 `KeyE`。
- 携带/掉落：随机携带者 `carrierSelectRule: 'player'`，允许掉落 `allowDrop`，丢弃键 `KeyZ`；
  `dropTimeout: 0`（掉地上的炸弹保持原地，不再自动挪回包点）。
- 边界规则：`attackerElimAutoWin: false`（攻方被全歼不立即判负，视炸弹状态）、
  `defuseTieWinsDefender: true`。

### 2.2 模块与职责（严格单一职责 + 事件解耦）
- `js/sd-bomb.js` — 炸弹**全生命周期状态机**（纯粹、配置驱动、发事件）：
  `UNASSIGNED → CARRIED ⇄ DROPPED`、`CARRIED → PLANTING → PLANTED → DEFUSING → DEFUSED`、
  `PLANTED → DETONATED`。只管炸弹自身状态与计时，不碰场景/胜负。
- `js/sd-match.js` — 回合/对局管理器：`BUY → LIVE → RESULT` 阶段、回合钟、单命制胜负
  （含全歼分支）、半场交换、先赢 N 回合的整局判定。通过 hooks 解耦，不直接生成单位或操作场景。
- `js/sd-field.js` — 炸弹「物理侧」：把抽象状态映射为场景网格（掉落/已安装时闪烁），
  把玩家按 E 的输入翻译成状态转移；负责「携带者阵亡即掉落」（玩家与 AI 一视同仁）；
  构建 A/B 世界标记与**屏幕投影的 A/B 指引标记**。必须在 `SdMatch.update` 前每帧运行。
- `js/sd-spawn.js` — 单命制出生：回合开始重规划 A/B 包点、满编重投双方 AI、把玩家复位回满血，
  并为攻方随机选出炸弹携带者。以 hooks（setupRound / pickCarrier）交给 SdMatch。
- `js/sd-stats.js` — 个人战绩与积分。核心难点：AI 名单每回合重建、id 会漂移，故用一组
  **固定席位（seat）**把每回合新单位映射到稳定身份，从而跨回合累计战绩。
- `js/sd-result.js` — 两层结算：Layer1 回合横幅（结果+原因+比分+回合 MVP），
  Layer2 对局计分板（最终比分+双队计分板+全局 MVP，常驻至手动/倒计时返回）。DOM 自建。
- `js/sd-ui.js` — HUD：复用顶栏比分/分段条/时钟，补充阶段横幅与**双方存活/阵亡状态条**。
- `js/sd-net.js` — 联机同步。

### 2.3 本周迭代做的事

**第 1 轮**
1. **守方受击反击**（`ai.js`）：`_damageUnit` 记录 `unit._aggroFrom / _aggroAt`（'player' 或攻击者对象）；
   新增 `_recentAttacker(unit)` 把「刚才打我的人」解析为可交战目标；`_sdDefendSite` 在没有
   可见目标时转身回击最近的攻击者，不再站桩挨打。
2. **A/B 世界指引标记**（`sd-field.js`）：新增屏幕投影的悬浮 A/B 包点标记（`state.siteHud`），
   穿墙可见、越界贴边，接入 start/stop/update 生命周期。
3. **顶栏存活/阵亡状态条**（`index.html` + `style.css` + `sd-ui.js`）：计时下方显示双方存活/阵亡
   图标 + 中央当前包点字母；`_syncStatus` 渲染，离开时隐藏。

**第 2 轮**
4. **存活/阵亡图标改为小人/骷髅**（`sd-ui.js` + `style.css`）：用内联 SVG（`fill=currentColor`）
   画站立小人（存活，按队色 + 发光）与骷髅（阵亡，灰；眼窝/鼻孔用 evenodd 挖空），替换旧的方块图标。
5. **守方 AI 卡点修复**（`ai.js` `_sdDefendSite`）：见 2.4。

**跨模式**
6. **AI 行进间开火**（`ai.js`）：见第 3 节。

### 2.4 解决的问题
- **守方只巡逻、不去包点**
  - 现象：守方常在图上游走，不占 A/B。
  - 根因：`_sdDefendSite` 中拴绳半径 `SD_DEF_LEASH_R = 24` 几乎恒成立，只要 40 范围内出现一个
    「打不到的」攻方（躲掩体/无视线），就触发 `_engage → _advanceOn` 把守方推向目标，表现为游走。
    且 `_nearestHostile` 不做视线判定，会「追」看不见的敌人。
  - 修复：**能打到才就地还击**；打不到时**绝不追出包点**——被引出先归位，已在点上则钉住盯防
    进攻通道，等敌人进入射界再开火。`_recentAttacker` 受击回击仍作兜底。

---

## 3. 跨模式 AI 改进：行进间开火（move-and-shoot）

### 问题
AI 只在 `engage` 状态开火；`advance`（赶路）状态的 `_advanceOn` 只移动、完全不开火。
因此奔赴目标（去 A/B 点、拆弹、回防、追击、走热区）途中，即便敌人就在射程内且有视线，
单位也只会闷头走路挨打——「赶路过程中被击杀」。

### 修复（`ai.js`）
- 新增 `_opportunityFire(unit, dt)`：在 `_advanceOn` 移动后调用，扫描武器射程内最近的敌人，
  交给既有 `_tryShoot` 统一处理冷却/视线/命中/曳光。**只补一枪，不改变走位方向**（真正边走边打）。
- 新增 `_shootTargetOf(threat)`：把 `_nearestHostile` 的目标描述子转成 `_tryShoot` 需要的射击目标
  （玩家会带上 `alive`/`object`）；`_engage` 复用它以消除重复代码。
- 开火节奏与 `engage` 一致（同为 brain 帧触发），行进间 DPS 与站桩交火相当，不失衡。
- 影响面：`_advanceOn` 是所有「赶路」的公共入口，故该行为对 TDM 与爆破同时生效。

---

## 4. 关键常量与入口速查

- 模式表 / 参数单点：`js/gamemodes.js`（`GameModes.param`、`isTdm()`、`isSd()`）。
- TDM：`tdm-match.js`（`registerKill`）、`tdm-spawn.js`（安全评分出生）、`tdm-ui.js`。
- 爆破：`sd-bomb.js`（状态机）、`sd-match.js`（`BUY/LIVE/RESULT`、`attackerTeam/defenderTeam`、
  `halfSwapAfter=6`）、`sd-field.js`（`plantKey=E` / `dropKey=Z` / A/B 标记）、`sd-spawn.js`、
  `sd-stats.js`（固定席位）、`sd-result.js`（两层结算）、`sd-ui.js`（`_syncStatus`）。
- AI：`ai.js` — `_updateBaseSoldier`（爆破决策序）、`_sdDefendSite / _sdDefendPre`（守点）、
  `_engage / _advanceOn / _opportunityFire / _shootTargetOf`（战斗与行进间开火）、
  `_damageUnit / _recentAttacker`（受击记忆与反击）。

## 5. 后续待办 / 已知项
- 图标与状态条改动需硬刷新（Ctrl+F5）才会加载最新 JS/CSS。
- 模式表中 `reserved-2`（待定模式）为占位，位置已预留。