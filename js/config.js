/**
 * 运行时配置（部署前改这里）
 *
 * VF_PVP_RELAY: 腾讯云中继根地址（不要末尾斜杠）
 *   例：'http://1.2.3.4:9000'
 *   例：'https://service-xxxx.gz.apigw.tencentcs.com/release'
 * 留空 '' 则回退到当前站点的 /api/pvp-bus（Netlify）
 */
window.VF_PVP_RELAY = '';
