# 公网上线通道 — 缺口清单

> 目标：不同网络环境下也能联机，做成可上线、供所有人玩的联机游戏。  
> 范围：基于当前「体素战争 + 本地开发包 threejs-3d-multiplayer（Kubee）」现状。  
> 更新日期：2026-07-30

---

## 0. 一句话结论

| 已有 | 缺少 |
|------|------|
| 游戏客户端、本地 Kubee 房间联机、同机双窗口验证通过 | **公网 7×24 房间/中继服务 + 正式发布入口 + HTTPS 公网域名** |

本地开发包安装正常；卡住的不是「再装依赖」，而是 **上线通道尚未打通**。

---

## 1. 当前已经具备（对照用）

### 1.1 本机开发

- [x] Node / pnpm 可用
- [x] `threejs-3d-multiplayer` 依赖已安装（`pnpm install` 通过）
- [x] `npx kubee game dev` 可启动（端口 15180）
- [x] `game/server` 房间消息转发逻辑（本地）
- [x] `game/client` 外壳 + iframe 加载游戏
- [x] `public/vf` 游戏静态文件 + `kubee-bridge.js` + Kubee 版 PVP
- [x] 同机双窗口联机验证通过

### 1.2 已有公网静态站（仅前端，无联机房间）

- [x] Netlify 账号已登录
- [x] 站点：`voxel-frontline-pve`
- [x] URL：https://voxel-frontline-pve.netlify.app
- [ ] **该站不能提供跨网房间联机**（没有常驻 WebSocket 房间服务）

### 1.3 CLI 能力边界（重要）

本地 Kubee CLI **仅有**：

```text
kubee game dev
```

- [ ] **没有** `kubee publish` / 一键公网发布命令  
- [ ] `kubee.json` 里 `"published": true` **不等于**已经上线给全世界

---

## 2. 公网上线通道 — 还缺什么（总表）

下面每一项都是「上线给所有人跨网玩」的硬缺口。

| 编号 | 缺口 | 为什么必须有 | 当前状态 |
|------|------|--------------|----------|
| G1 | **公网可访问的游戏入口 URL** | 玩家不能用 `127.0.0.1` | 仅有 Netlify 静态站；未绑定「带房间服的正式入口」 |
| G2 | **公网 7×24 房间/中继服务** | 跨网双方消息必须经过公网中继（或平台房间） | 仅本机 `kubee game dev`，关机即断 |
| G3 | **HTTPS（合法证书）** | 浏览器安全策略；公网联机标配 | 本地 http；Netlify 静态有 https，但无房间服 |
| G4 | **正式发布流程** | 把「开发包」变成「线上版本」 | CLI 无 publish；未走平台发布 / 未自建部署 |
| G5 | **房间发现方式** | 陌生人如何进同一局 | 本地靠「同一 15180 页面」；线上缺匹配/公开房间链接产品化 |
| G6 | **运维与守护** | 进程挂了要自动起、要有日志 | 无云主机守护 / 无平台托管确认 |
| G7 | **跨网连通策略定稿** | 选平台发布 or 自建 or Netlify+外挂中继 | **未选定** |

---

## 3. 三条上线路径 — 各自还缺的具体东西

必须先选一条路径；不同路径缺的东西不同。

---

### 路径 A：Kubee / 平台正式发布（有权限时优先）

适合：学校/公司/官方提供 Kubee 多人发布能力。

#### A. 还缺的账号与权限

- [ ] Kubee（或对应平台）**可登录账号**
- [ ] 该账号具备 **多人游戏发布 / 公开房间** 权限
- [ ] 官方后台入口地址（发布页 / 控制台 URL）
- [ ] 发布文档或内部教程（本机 CLI **没有** publish）

#### A. 还缺的产物

- [ ] 平台生成的 **公开房间 / 游戏链接**（所有玩家打开同一链接）
- [ ] 确认链接是否：
  - [ ] 跨运营商可访问
  - [ ] 自带房间 WebSocket
  - [ ] 支持至少 2 人同时在线

#### A. 还缺的验收

- [ ] 手机 4G/5G 打开公开链接能否进房
- [ ] 另一台不同宽带电脑能否进同一房
- [ ] 断线重连、房间满员提示是否符合预期

#### A. 你需要提供给我的信息

1. 平台登录方式 / 是否已有账号  
2. 发布后台截图或文档链接  
3. 发布后拿到的公开 URL  

> 若 **没有平台发布权限**，路径 A 走不通，改选 B 或 C。

---

### 路径 B：自建公网服务器（完全自主）

适合：有云服务器预算，不依赖 Kubee 平台。

#### B. 还缺的基础设施

| 项 | 具体要求 | 状态 |
|----|----------|------|
| 云服务器 | 公网 IP；建议 2核4G 起；系统 Ubuntu 22.04 等 | [ ] 未采购 |
| 域名 | 如 `game.xxx.com`（强烈建议） | [ ] 未准备 |
| HTTPS | Let’s Encrypt / Cloudflare | [ ] 未配置 |
| 防火墙 | 放行 80/443（及中继端口若单独暴露） | [ ] 未配置 |
| 进程守护 | pm2 / systemd / docker compose | [ ] 未配置 |

#### B. 还缺的软件部署

- [ ] 将「静态前端 + 房间中继」部署到服务器  
  - 前端：可用现有 `public/vf` + 外壳，或纯静态站  
  - 中继：基于现有 `game/server` 思路改造为可公网监听的 Node 服务  
- [ ] 环境变量 / 配置：公网 WebSocket 地址写入客户端  
- [ ] 开机自启、日志目录、简单健康检查（`/health`）  
- [ ] （可选）Nginx 反代：`https://域名` → 静态；`wss://域名/ws` → 中继  

#### B. 还缺的代码改造（相对现状）

- [ ] 客户端不再依赖「仅本机 Kubee iframe 外壳」才能联机（或服务器上完整跑 Kubee 兼容运行时）  
- [ ] 明确生产环境连接地址（不可写死 `127.0.0.1`）  
- [ ] 生产构建与发布脚本（一键上传/重启）  

#### B. 你需要提供给我的信息

1. 云厂商与是否已有机器（IP / SSH）  
2. 域名是否已备案（国内机房通常需要）  
3. 是否接受 docker 部署  

---

### 路径 C：Netlify 继续托管前端 + 外挂公网中继（折中）

适合：想继续用已有 Netlify 站，另外部署联机服务。

#### C. 已有

- [x] Netlify 静态站：https://voxel-frontline-pve.netlify.app  

#### C. 还缺

| 项 | 说明 | 状态 |
|----|------|------|
| 独立 WebSocket 中继 | 不能指望 Netlify 静态页当房间服；需另外部署（云主机 / Fly.io / Railway / 自建 Node 等） | [ ] |
| 中继公网地址 | 如 `wss://relay.xxx.com` | [ ] |
| 客户端配置 | 写入中继 URL（可参考 `VF_PVP_RELAY` 思路，并接到现有 PVP） | [ ] |
| CORS / 安全 | 仅允许你的游戏域名连接中继 | [ ] |
| 最新游戏发布到 Netlify | 把含联机配置的最新 `体素战争` 部署上去 | [ ] 需确认是否已是 Kubee/中继版最新包 |
| Kubee 外壳是否保留 | 若不用 Kubee，需确认大厅「进入联机」改走纯中继模式 | [ ] 产品决策 |

#### C. 注意

- Netlify Functions **不适合**做长时间 WebSocket 房间（应用场景不对）。  
- 必须有一个 **常驻进程** 的中继，和静态站分开。

#### C. 你需要提供给我的信息

1. 中继打算放哪（已有服务器 / 需要推荐免费档）  
2. 正式前端是否就用 `voxel-frontline-pve.netlify.app`  

---

## 4. 无论哪条路径，上线前建议补齐的产品/技术项

这些不是「网络通」的绝对前提，但是「给所有人玩」强烈建议：

| 项 | 说明 | 状态 |
|----|------|------|
| 进房方式 | 公开链接 / 房间码 / 一键匹配（至少一种对陌生人友好） | [ ] 仅有「同一链接进房」雏形 |
| 人数与满房 | 显示 1/2、满员拒绝或排队 | [ ] 需验收 |
| 断线重连 | 刷新/闪断后能否回房 | [ ] 本地有部分逻辑，公网未验 |
| 版本一致性 | 双方客户端版本不一致时的提示 | [ ] 无 |
| AI 同步策略 | 关闭 AI / 或服务器权威（否则两边兵线可能不一致） | [ ] 未定 |
| 主机权威规则文档 | 伤害、核心血量以谁为准（已有部分主机权威） | [ ] 需写成验收用例 |
| 滥用防护 | 限速、房间数上限、简单鉴权 | [ ] 无 |
| 监控告警 | 进程挂掉通知、基础 QPS/在线人数 | [ ] 无 |
| 隐私与条款 | 若公开给所有人，是否需要用户协议（视发布渠道） | [ ] 未定 |

---

## 5. 「通道」拆成可交付物清单（验收用）

上线通道打通的最低验收标准：

1. **任意两台不同公网设备**（例如：家宽电脑 + 手机流量）打开 **同一个公开 URL**  
2. 都能进入联机大厅，一人房主一人访客  
3. 双方准备 → 开始 → 能互相看见、能造成伤害 / 同步建造  
4. 关闭开发者本机后，**线上仍可继续联机**（不依赖你电脑上的 `kubee game dev`）  

当前：**1–3 仅在同机 localhost 验证过；第 4 条完全未满足。**

---

## 6. 你现在需要拍板 / 准备的东西（最短路径）

请按顺序确认：

### 第一步：选路径（必选）

- [ ] **A** — 有 Kubee/平台发布权限  
- [ ] **B** — 自建云服务器  
- [ ] **C** — Netlify 前端 + 外挂中继  

### 第二步：按路径准备物料

| 若选 | 请准备 |
|------|--------|
| A | 平台账号、发布后台、文档；发布后公开链接 |
| B | 云服务器 SSH、域名（国内还要备案情况）、是否用 docker |
| C | 中继托管位置；确认 Netlify 站点是否作为正式前端 |

### 第三步：我方可继续落地的工作（选定后）

- 改客户端连接公网中继/平台房间  
- 写部署脚本 / Nginx / pm2 配置  
- 部署最新游戏到 Netlify（若走 C）  
- 出跨网测试验收步骤  

---

## 7. 明确「不是缺口」的东西（避免重复劳动）

以下 **不必** 为了「公网上线」再装一遍：

- 重新 `pnpm install` 本地开发包（已正常）  
- 再写一套同机 PVP 大厅（已通）  
- 指望只把静态文件丢到 Netlify 就自动跨网联机（**不够**）  

---

## 8. 相关路径速查

| 用途 | 路径 |
|------|------|
| 游戏工程 | `C:\Users\Lenovo\Desktop\体素战争` |
| Kubee 多人模板 | `C:\Users\Lenovo\Desktop\本地开发包\本地开发包\threejs-3d-multiplayer` |
| 本地联机说明 | `体素战争\KUBEE联机说明.md` |
| 模板联机说明 | `threejs-3d-multiplayer\体素前线-联机说明.md` |
| 现有静态站 | https://voxel-frontline-pve.netlify.app |
| 本地开发地址 | http://127.0.0.1:15180 |

---

## 9. 状态摘要（可贴到汇报）

```text
【已完成】本地 Kubee 多人开发环境；游戏 Kubee 联机逻辑；同机双窗口联机；
         Netlify 静态站（仅页面，无房间服务）。

【未完成 / 缺口】公网 7×24 房间中继或平台发布；正式公开入口 URL；
               HTTPS 生产部署；跨网验收；陌生人进房产品化；运维守护。

【阻塞决策】上线路径未选：A 平台发布 / B 自建服务器 / C Netlify+外挂中继。
```
